% STATUT : À COMPLÉTER. Le tableau détaillé vit au chapitre robustesse ;
% cette annexe porte la version exhaustive, paramètre par paramètre, avec
% la source exacte (fichier, script) de chaque valeur.
\chapter{Inventaire complet des paramètres}
\label{ann:parametres}

\textit{[Annexe à compléter : reprendre le tableau du chapitre~\ref{ch:robustesse} en
version exhaustive, en ajoutant pour chaque paramètre le script et le fichier qui le
produisent, de sorte que tout chiffre du mémoire soit traçable.]}

% Colonnes cibles :
%   Paramètre | Symbole | Valeur | Nature (calibré / posé / semi-ancré)
%   | Source (fichier + script) | Sensibilité du SCR
%
% Règle : aucun chiffre ne doit apparaître dans le mémoire sans une ligne
% dans cette annexe. C'est ce qui rend la revendication d'honnêteté
% vérifiable plutôt que déclarative.
